{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch server.ts",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "docker exec -it bun-server bunx drizzle-kit migrate",
    "db:script": "docker exec -it bun-server bun run src/index.ts",
    "db:studio": "docker exec -it bun-server bunx drizzle-kit studio --host 0.0.0.0",
    "db:command": "docker exec -it marketplace_db psql -U postgres -d online_marketplace"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6",
    "@types/pg": "^8.16.0",
    "drizzle-kit": "^0.31.8",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "express": "^5.2.1",
    "nodemon": "^3.1.11",
    "pg": "^8.16.3"
  }
}
